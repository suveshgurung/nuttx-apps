include $(APPDIR)/Make.defs

PROGNAME = $(CONFIG_EXAMPLES_UART_COMM_PROGNAME)
PRIORITY = $(CONFIG_EXAMPLES_UART_COMM_PRIORITY)
STACKSIZE = $(CONFIG_EXAMPLES_UART_COMM_STACKSIZE)
MODULE = $(CONFIG_EXAMPLES_UART_COMM)

MAINSRC = uart_comm.c

include $(APPDIR)/Application.mk
